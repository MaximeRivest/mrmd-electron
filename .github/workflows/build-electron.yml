name: Build Electron App

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
          - os: macos-latest
            platform: mac
          - os: windows-latest
            platform: win

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Clone sibling packages (they use file: references)
      - name: Clone mrmd-editor
        run: git clone --depth 1 https://github.com/MaximeRivest/mrmd-editor.git ../mrmd-editor

      - name: Clone mrmd-sync
        run: git clone --depth 1 https://github.com/MaximeRivest/mrmd-sync.git ../mrmd-sync

      - name: Clone mrmd-monitor
        run: git clone --depth 1 https://github.com/MaximeRivest/mrmd-monitor.git ../mrmd-monitor

      - name: Clone mrmd-project
        run: git clone --depth 1 https://github.com/MaximeRivest/mrmd-project.git ../mrmd-project

      - name: Clone mrmd-jupyter-bridge
        run: git clone --depth 1 https://github.com/MaximeRivest/mrmd-jupyter-bridge.git ../mrmd-jupyter-bridge

      # Build mrmd-editor (required by electron)
      - name: Build mrmd-editor
        run: |
          cd ../mrmd-editor
          npm install
          npm run build

      # Install sibling deps (use npm install - lock files may be out of sync)
      - name: Install sibling packages
        run: |
          cd ../mrmd-sync && npm install
          cd ../mrmd-monitor && npm install
          cd ../mrmd-project && npm install
          cd ../mrmd-jupyter-bridge && npm install
        shell: bash

      # Copy sibling packages into project for bundling
      # electron-builder extraResources has issues with filters, so we copy manually
      - name: Stage sibling packages for bundling
        run: |
          mkdir -p sibling-packages
          # Copy with node_modules
          cp -r ../mrmd-sync sibling-packages/
          cp -r ../mrmd-monitor sibling-packages/
          cp -r ../mrmd-project sibling-packages/
          cp -r ../mrmd-jupyter-bridge sibling-packages/
          cp -r ../mrmd-editor/dist sibling-packages/mrmd-editor-dist
          # Verify
          echo "=== Staged packages ===" && ls -la sibling-packages/
          echo "=== mrmd-monitor contents ===" && ls sibling-packages/mrmd-monitor/
          echo "=== mrmd-monitor/node_modules ===" && ls sibling-packages/mrmd-monitor/node_modules/ 2>/dev/null | head -5 || echo "WARNING: No node_modules!"

      # Build electron
      - name: Install electron dependencies
        run: npm ci

      - name: Build electron app
        run: npm run build:${{ matrix.platform }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Upload artifacts
      - name: Upload Linux artifacts
        if: matrix.platform == 'linux'
        uses: actions/upload-artifact@v4
        with:
          name: mrmd-linux
          path: |
            dist/*.AppImage
            dist/*.deb
          if-no-files-found: warn

      - name: Upload macOS artifacts
        if: matrix.platform == 'mac'
        uses: actions/upload-artifact@v4
        with:
          name: mrmd-macos
          path: |
            dist/*.dmg
            dist/*.zip
          if-no-files-found: warn

      - name: Upload Windows artifacts
        if: matrix.platform == 'win'
        uses: actions/upload-artifact@v4
        with:
          name: mrmd-windows
          path: |
            dist/*.exe
          if-no-files-found: warn

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          generate_release_notes: true
          files: |
            artifacts/mrmd-linux/*
            artifacts/mrmd-macos/*
            artifacts/mrmd-windows/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
